# Demos

- `witness-debugger.cast` — the witness debugger stepping through the
  canonical RC001 divergent-barrier hang and the RC002 mask mismatch,
  recorded from the real TUI over the checked-in fixtures
  (`scripts/record-witness-demo.sh`, fully offline). Play with
  `asciinema play docs/demo/witness-debugger.cast` or any asciicast-v2
  player; re-record after intentional UI changes.
