[package]
name = "reconverge-core"
description = "Dialect-agnostic divergence analysis engine for SIMT kernels (MIR dataflow)"
version.workspace = true
edition.workspace = true
# MSRV of the library crates; the driver alone needs rust-toolchain.toml's nightly.
rust-version = "1.88"
license.workspace = true
repository.workspace = true
publish.workspace = true
readme = "README.md"
keywords = ["gpu", "simt", "dataflow", "static-analysis"]
categories = ["development-tools"]
