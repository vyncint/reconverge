[package]
name = "reconverge-driver"
description = "Compiler driver that runs reconverge analyses and emits versioned artifacts"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
reconverge-core = { path = "../reconverge-core", version = "0.1.1" }
reconverge-dialect-oxide = { path = "../reconverge-dialect-oxide", version = "0.1.1" }
reconverge-witness = { path = "../reconverge-witness", version = "0.1.1" }
reconverge-artifacts = { path = "../reconverge-artifacts", version = "0.1.1" }
serde_json = { workspace = true }
